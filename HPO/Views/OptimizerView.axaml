<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             xmlns:vm="using:HeatProductionOptimization.ViewModels"
             x:Class="HeatProductionOptimization.Views.OptimizerView"
             x:DataType="vm:OptimizerViewModel">
    <Grid Margin="30" RowDefinitions="Auto,Auto,Auto,Auto,Auto,*,Auto">
        <TextBlock Grid.Row="0"
                   Text="Heat Production Optimizer / Optimizer"
                   FontSize="40"
                   FontWeight="Bold"
                   HorizontalAlignment="Left"
                   Margin="0,0,0,30"/>
        
        <!-- Algorithm Selection Section -->
        <Border Grid.Row="1" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1" 
                CornerRadius="5" 
                Padding="20" 
                Margin="0,0,0,20"
                Background="#F5F5F5">
            <StackPanel>
                <TextBlock Text="Algorithm Configuration" 
                           FontSize="22" 
                           FontWeight="SemiBold" 
                           Margin="0,0,0,15"/>
                
                <TextBlock Text="Heat Demand:" 
                           FontSize="16" 
                           FontWeight="Medium" 
                           Margin="0,0,0,15"/>
                <NumericUpDown Minimum="0.1" Value="{Binding HeatNeeded}" Increment="0.1" Margin="5,0,0,15" Maximum="{Binding MaxHeat}" Width="150" HorizontalAlignment="Left"/>
                
                <TextBlock Text="Optimization Parameters:" 
                           FontSize="16" 
                           FontWeight="Medium" 
                           Margin="0,0,0,10"/>
                
                <Grid ColumnDefinitions="*,*,*" RowDefinitions="*" Margin="10,0">
                    <CheckBox Grid.Row="0" Grid.Column="0" 
                              Name="CheckBox1"
                              Content="Production Cost" 
                              IsChecked="True"
                              Click=""
                              Margin="0,5"/>
                    
                    <CheckBox Grid.Row="0" Grid.Column="1" 
                              Name="CheckBox2"
                              Content="Fuel Consumption" 
                              IsChecked="True"
                              Click=""
                              Margin="0,5"/>
                    
                    <CheckBox Grid.Row="0" Grid.Column="2" 
                              Name="CheckBox3"
                              Content="CO2 Emissions" 
                              IsChecked="True"
                              Click=""
                              Margin="0,5"/>
                </Grid>     
            </StackPanel>
        </Border>
        
        <!-- Run Optimization Button -->
        <Button Grid.Row="2" 
                Content="Run Optimization" 
                HorizontalAlignment="Center"
                Padding="20,10"
                Margin="0,10,0,30"
                Command="{Binding RunOptimizationCommand}"/>
        
        <!-- Optimization Status -->
        <TextBlock Grid.Row="3" 
                   Text="Status: Ready to optimize" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>
        
        <!-- Navigation Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Spacing="20"
                    Margin="0,20,0,0">
            <Button Content="View Optimization Results" 
                    Padding="15,10"
                    Click="ViewResults_Click"/>
            
            <Button Content="Visualize Data" 
                    Padding="15,10"
                    Click="VisualizeData_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
